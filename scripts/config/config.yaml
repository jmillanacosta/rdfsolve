# RDFSolve pipeline configuration

# Base IRI for resources that rdfsolve identifies
base_uri: https://w3id.org/rdfsolve/

# Timeout settings (seconds)
timeout: 300
delay: 0.5

# QLever settings
qlever:
  image: docker://docker.io/adfreiburg/qlever:latest
  startup_timeout: 10000
  base_port: 7019
